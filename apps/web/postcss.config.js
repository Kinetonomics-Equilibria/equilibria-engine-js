// This project uses plain CSS — no PostCSS plugins.
// Present only to stop PostCSS's config search from walking up out of the
// repo and picking up an unrelated config in a parent directory.
export default { plugins: {} };
